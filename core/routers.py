from rest_framework_extensions.routers import ExtendedDefaultRouter

# from rest_framework_friendship import views

from . import viewsets

# router = ExtendedDefaultRouter()

# router.register(r'profile', viewsets.ProfileDetailViewSets, 'profiles')
# router.register(r'role', viewsets.UserRoleDetailViewSet, 'roles')
# router.register(r'request', views.FriendshipRequestViewSet, 'fr-request')
# router.register(r'request-inbound', views.FriendshipRequestInboundViewSet, 'fr-request-inbound')
# router.register(r'request-outbound', views.FriendshipRequestOutboundViewSet, 'fr-request-outbound')
# router.register(r'friends', views.FriendViewSet, 'friends')
